; Üretim OPcache modu: dosya değişikliği algılama KAPALI (maksimum hız).
; Bunun bedeli: her "git pull" sonrası PHP eski bytecode'u sunmaya devam eder.
; Deploy sonrası şu komut ŞART:
;   docker compose -f docker-compose.prod.yml restart web
opcache.validate_timestamps=0
